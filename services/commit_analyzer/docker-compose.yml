version: '3.8'

services:
  api:
    build:
      context: ../..
      dockerfile: services/commit_analyzer/Dockerfile
    ports:
      - "8008:8008"
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/testlens

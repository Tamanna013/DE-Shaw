version: '3.8'

services:
  api:
    build:
      context: ../..
      dockerfile: services/llm_orchestration/Dockerfile
    ports:
      - "8006:8006"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - LOG_LLM_PAYLOADS=false
